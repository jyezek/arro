// /interview/new — start a fresh practice session (no specific job)
// This just re-exports the same interview session screen with no jobId
import InterviewScreen from './[id]'
export default InterviewScreen
